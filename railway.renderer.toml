[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.renderer"

[deploy]
startCommand = "sh -c 'RENDERER_HOST=:: RENDERER_PORT=$PORT diagrams-renderer-server'"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
