[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.renderer"

[deploy]
startCommand = "sh -c 'RENDERER_PORT=$PORT diagrams-renderer-server'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
